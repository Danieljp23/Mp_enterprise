specVersion: "v1beta"
serviceId: "maple"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "maple-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
